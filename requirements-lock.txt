# Pinned dependency versions for CRITICAL-MM.
# These are the exact versions of the environment that produced the released
# baselines and checkpoints. Use with Python 3.11.
#
# NOTE on PyTorch/CUDA: the reference environment used torch 2.12.0 built for
# CUDA 13.0 (torch==2.12.0+cu130). To reproduce that exact GPU build, install
# torch from the matching PyTorch index, e.g.:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cu130
# or pick the index matching your own CUDA / CPU-only setup.

# Core
polars==1.40.1
numpy==1.26.4
pandas==2.0.0
scikit-learn==1.8.0
pyarrow==24.0.0
pydantic==2.13.4
typer==0.25.1
xxhash==3.6.0
PyYAML==6.0.3

# Training (deep + gradient-boosted baselines)
torch==2.12.0
pytorch-lightning==2.6.1
pytorch-ignite==0.5.4
torchmetrics==1.9.0
einops==0.8.2
lightgbm==4.6.0

# Multimodal (clinical-notes / diagnosis-code fusion) -- optional
transformers==5.9.0
tokenizers==0.22.2
safetensors==0.7.0
huggingface-hub==1.17.0
